<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Test Results</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background-color: #f5f5f5;
    }

    h1 {
      color: #333;
    }

    .test-case {
      background: white;
      padding: 15px;
      margin: 10px 0;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
    }

    button {
      background-color: #007acc;
      color: white;
      border: none;
      padding: 8px 12px;
      border-radius: 4px;
      cursor: pointer;

    }

    button:hover {
      background-color: #005fa3;
    }

    body {
      font-family: sans-serif;
      padding: 20px;
    }

    pre {
      white-space: pre-wrap;
      background-color: rgb(221, 221, 221);
      padding: 6px;
    }

    strong {
      font-weight: bold;
      font-size: 1.2em;
    }

    .success {
      color: green;
      font-size: 0.8em;
    }

    .fail {
      color: red;
      font-size: 0.8em;
    }
  </style>

</head>

<body>

  <div class="title"><strong><%= data.title %></strong></div>
  <div id="result"></div>

  <strong>Input</strong>
  <pre><%= data.input %></pre>
  <strong>Output</strong>
  <pre><%= data.output %></pre>
  <% if (data.viewKind === "beforeExec") { %>
  <strong>Your Output</strong>
  <% } else if (data.viewKind === "success") { %>
  <strong>Your Output</strong>
  <span class="success">✅ Passed </span>
  <div>
    <pre><%= data.actual_output %></pre>
  </div>
  <% } else if (data.viewKind === "fail") {%>
  <strong>Your Output</strong>
  <span class="fail">❌ Failed </span>
  <div>
    <pre><%= data.actual_output %></pre>
  </div>
  <% } else { %>
  <div> illeagal state</div>
  <% } %>

</body>

</html>